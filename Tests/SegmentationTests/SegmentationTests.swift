import XCTest
@testable import Segmentation

final class SegmentationTests: XCTestCase {

    func testProcessFrame() async throws {
        throw XCTSkip("Stage 2 (Person Segmentation) not yet implemented")
    }
}
